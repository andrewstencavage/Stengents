"""Reusable, agent-neutral utilities: model source, observation, turn logging.

This package is a leaf: it must not import the harness, CLI, or any specific
agent. Both the `stengents` harness and `farm_system` agents depend on it.
"""
